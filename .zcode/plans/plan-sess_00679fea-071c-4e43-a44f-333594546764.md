# Rencana Perbaikan Bebas-Blocker Kanban Outreach Sesuai Arahan Pak Nell

1. **src/app/api/outreach/campaigns/route.ts**:
   - Hapus kewajiban `accountId` saat membuat kampanye baru. Izinkan `accountId` null.

2. **src/app/dashboard/outreach/new/page.tsx**:
   - Hapus pemblokir `disabled` pada tombol submit dan hapus validasi `selectedAccountId`.
   - Jadikan pemilihan sender mailbox murni opsional.
   - User bisa langsung mengisi Target Role, Lokasi, dan klik "Create Campaign & Source Leads" tanpa hambatan.

3. **src/app/api/outreach/campaigns/[id]/scrape/route.ts**:
   - Pastikan scraping Apify membaca token `process.env.APIFY_API_TOKEN` server secara langsung dan tidak tergantung pada `campaign.account`.

4. **src/app/dashboard/outreach/[id]/page.tsx**:
   - Pastikan tombol `🔍 Source More`, `🛡️ Verify Mailboxes`, dan `+ Push to CRM` berjalan lancar tanpa konfigurasi sender.
   - Tambahkan tombol **`➕ Push All to Kanban CRM`** untuk memindahkan seluruh lead terverifikasi ke papan CRM dalam 1 klik.

5. **Verifikasi Build & Uji Alur End-to-End**:
   - Jalankan `npx next build` dan uji alur create -> scrape -> verify -> push to crm.